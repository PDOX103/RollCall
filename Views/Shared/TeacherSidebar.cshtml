<div id="sidebar" class="sidebar sidebar-hidden animate__animated">
    <h4>📚 Teacher Panel</h4>
    <a href="#">🏠 Dashboard</a>
    <a href="#">📖 Courses</a>
    <button type="button" class="mode-btn" onclick="toggleMode()">🌙 Dark Mode</button>
</div>

<style>
    .sidebar {
        width: 250px;
        background: #fff;
        padding: 25px 20px;
        transition: transform 0.3s ease-in-out;
        box-shadow: 2px 0 15px rgba(0,0,0,0.2);
        z-index: 1001;
        position: fixed;
        top: 0;
        left: 0;
        height: 100%;
    }

        .sidebar h4 {
            font-weight: bold;
            margin-bottom: 30px;
            color: #2c3e50;
            font-size: 20px;
            text-align: center;
        }

        .sidebar a, .sidebar .mode-btn {
            display: flex;
            align-items: center;
            padding: 12px 15px;
            margin-bottom: 15px;
            border-radius: 10px;
            background: none;
            border: none;
            color: #2c3e50;
            text-decoration: none;
            font-size: 16px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

            .sidebar a:hover, .sidebar .mode-btn:hover {
                background: #f1f1f1;
            }

    .sidebar-hidden {
        transform: translateX(-100%);
    }

    /* Overlay for mobile when sidebar is open */
    .sidebar-overlay {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0,0,0,0.4);
        z-index: 1000;
    }

    /* Dark Mode */
    body.dark-mode {
        background: #2c3e50 !important;
        color: #ecf0f1 !important;
    }

        body.dark-mode .sidebar {
            background: #34495e;
            color: #ecf0f1;
        }

            body.dark-mode .sidebar a,
            body.dark-mode .sidebar .mode-btn {
                color: #ecf0f1;
            }

    @@media (min-width: 768px) {
        .sidebar {
            position: relative;
            transform: none !important;
            box-shadow: 2px 0 10px rgba(0,0,0,0.1);
        }
    }
</style>
